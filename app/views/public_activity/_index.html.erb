<h1>Activity Feed</h1>
<% @activities.each do |activity| %>
	
	<% if activity.trackable %>
		<p><%= link_to activity.owner.username, activity.owner if activity.owner %>
		<%= render_activity activity %></p>
	<% end %>

<% end %>
